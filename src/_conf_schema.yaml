repo_owner:
  description: GitHub 仓库所有者
  type: string
  default: Kess66666
repo_name:
  description: GitHub 仓库名称
  type: string
  default: mihomo
main_workflow:
  description: 主构建工作流文件名
  type: string
  default: fully-compatible-build.yml
github_token:
  description: GitHub Personal Access Token（用于触发工作流和获取状态）
  type: string
  default: ''